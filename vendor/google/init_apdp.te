# This is temporary: Tracking revert on b/130895958
type init_apdp, domain;
type init_apdp_exec, exec_type, vendor_file_type, file_type;

userdebug_or_eng(`
# Shell script exec (toolbox)
allow init_apdp vendor_shell_exec:file r_file_perms;
allow init_apdp vendor_toolbox_exec:file rx_file_perms;
allow init_apdp vendor_file:file rx_file_perms;

# Find and write block device
allow init_apdp block_device:dir search;
allow init_apdp apdp_block_device:blk_file rw_file_perms;

init_daemon_domain(init_apdp)
')
